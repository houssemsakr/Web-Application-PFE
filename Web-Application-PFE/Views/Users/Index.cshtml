@model IEnumerable<Web_Application_PFE.ViewModels.UserViewModel>

@{
    ViewData["Title"] = "Index";
}
@section Styles {
    <link rel="stylesheet" href="~/css/Gestion-RFQ.css" />
}

<main role="main">
<div class="container-fluid">
    <!-- DataTales Example -->
    <div class="card shadow" style="margin-left:141px;width:90%">
      
        <div class="card-body" >
                <div class="card-header py-3 d-flex justify-content-end" style="background-color:white;border:none;margin-top:0px;margin-bottom:0px">

					<a class="btn btn-primary" asp-area="Identity" asp-page="/Account/Register">Create New</a>

				</div>
            <div class="table-responsive">
                <table class="table table-bordered" id="dataTable" width="10%" cellspacing="0">
                    <thead>
                        <tr>
                            <th>Nom</th>
                            <th>Prénom</th>
                            <th>Rôle</th>
                            <th>Email</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tfoot>
                        <tr>
                            <th>Nom</th>
                            <th>Prénom</th>
                            <th>Rôle</th>
                            <th>Email</th>
                            <th>Actions</th>
                        </tr>
                    </tfoot>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(modelItem => item.Nom)</td>
                                <td>@Html.DisplayFor(modelItem => item.Prenom)</td>
                                <td>@Html.DisplayFor(modelItem => item.Role)</td>
                                <td>@Html.DisplayFor(modelItem => item.Email)</td>
                                <td>
                                        <a class="btn btn-success" asp-action="Details" asp-route-id="@item.Id">
                                            <i class="bi bi-eye" style="font-size: 1rem;"></i>

                                        </a>
                                        <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">
                                            <i class="bi bi-trash" style="font-size: 1rem;"></i>

                                        </a>
                                        <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Id">
                                            <i class="bi bi-pencil" style="font-size: 1rem;"></i>

                                        </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
</main>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}